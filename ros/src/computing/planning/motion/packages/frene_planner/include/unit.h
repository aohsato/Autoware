#ifndef UNIT_H
#define UNIT_H

#include <math.h>

#define kmph2mps(kmph) ((kmph) / 3.6)
#define mps2kmph(mps) ((mps)*3.6)
#define rad2deg(deg) ((deg)*M_PI / 180.0)
#define deg2rad(rad) ((rad)*180.0 / M_PI)

#endif
